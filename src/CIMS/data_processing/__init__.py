"""
Data preparation scripts that build committed reference tables from raw downloads.

Each module here is runnable on its own and writes into ``data/``; they are kept in
the package (rather than under ``data/pipeline/``) so the build and the linters see
them as ordinary library code.
"""
